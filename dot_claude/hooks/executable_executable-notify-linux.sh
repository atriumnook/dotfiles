#!/usr/bin/env bash
# notify-linux.sh
# Notification hook variant for Linux native.
# Requires notify-send (libnotify-bin on Debian/Ubuntu, libnotify on Fedora/Arch).
# To use this: replace the contents of notify.sh with this file.

notify-send 'Claude Code' 'Claude Code needs your attention'

exit 0
